import java.util.Vector;

import org.apache.xmlrpc.XmlRpcClient;

public class Client {
	public static void main(String[] args) throws Exception{
		XmlRpcClient client = new XmlRpcClient("http://localhost:8080");
		Vector<Double> params = new Vector<Double>();
		params.add(new Double(21.0f));
        Double result = (Double) client.execute("TemperatureConversion.celsiusToFahrenheit", params);
        System.out.println(result);
	}
}
